# include <stdio.h>
# include <cs50.h>
# include <string.h>

int main(void)
{
    // Im bluiding blocks to make an pyramid blocks represented in #=i
    //then I need to establish height=8 now i need user input=GetInt
    //store vaulable=GetInt in 8
    string pyramid = "#";
    int Height = GetInt();
    {
        //number use in pyramid has to be non-negative=%d not greater than 23
        //If this happens ask user to quit game or start over
        if ("%d < 23, ")
        printf("Quit (Y/N)");
    }
    {   // have to place my variables in data form
        printf("%s, %d", pyramid, Height);
    }
    
    
}
